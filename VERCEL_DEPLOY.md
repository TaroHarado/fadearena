# 🚀 Деплой на Vercel - Итоговая инструкция

## ✅ Ваш API на Fly.io

**URL:** `https://fadearena-api.fly.dev`  
**Статус:** ✅ Работает (health check показывает degraded, но API отвечает)

## 📋 Пошаговая инструкция

### Шаг 1: Зайдите на Vercel

1. Откройте [vercel.com](https://vercel.com)
2. Войдите в аккаунт (или зарегистрируйтесь)
3. Нажмите **"Add New Project"**

### Шаг 2: Импортируйте репозиторий

1. Выберите ваш Git репозиторий (GitHub/GitLab/Bitbucket)
2. Если репозиторий не подключен - подключите его
3. Нажмите **"Import"**

### Шаг 3: Настройте проект

**⚠️ ВАЖНО - скопируйте эти настройки точно:**

| Параметр | Значение |
|----------|----------|
| **Framework Preset** | `Next.js` (автоматически) |
| **Root Directory** | `apps/web` ⚠️ **ОБЯЗАТЕЛЬНО!** |
| **Build Command** | `cd ../.. && pnpm install && pnpm --filter @fadearena/web build` |
| **Output Directory** | `.next` |
| **Install Command** | `pnpm install` |

### Шаг 4: Добавьте переменную окружения

1. Прокрутите вниз до раздела **"Environment Variables"**
2. Нажмите **"Add"**
3. Добавьте:

```
Name: NEXT_PUBLIC_API_URL
Value: https://fadearena-api.fly.dev
```

**⚠️ ВАЖНО:**
- Без `/api` в конце
- Без слэша `/` в конце
- Точный URL: `https://fadearena-api.fly.dev`

### Шаг 5: Деплой!

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-3 минуты)
3. После успешного деплоя вы получите URL вашего сайта

## ✅ Проверка после деплоя

### 1. Откройте ваш сайт

URL будет что-то вроде: `https://your-project.vercel.app`

### 2. Проверьте лендинг

- Должен открыться лендинг с логотипом
- Кнопки Twitter и Pump.fun должны быть видны

### 3. Проверьте дашборд

- Откройте `/dashboard`
- Должны загрузиться данные
- Чарты должны отображаться

### 4. Проверьте API запросы

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу
4. Проверьте запросы - они должны идти на `https://fadearena-api.fly.dev/api/...`
5. Статус должен быть 200 (OK)

## 🔧 Если что-то не работает

### API не отвечает (404/500)

1. Проверьте логи Fly.io:
   ```powershell
   flyctl logs --app fadearena-api
   ```

2. Проверьте, что машина запущена:
   ```powershell
   flyctl status --app fadearena-api
   ```

3. Если машина остановлена, запустите:
   ```powershell
   flyctl machine start <machine-id> --app fadearena-api
   ```

### CORS ошибки

Если видите CORS ошибки в консоли браузера, нужно обновить CORS настройки в API. Но обычно это не нужно, так как в коде уже есть `app.use(cors())`.

### Чарты не загружаются

1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Network tab - все ли запросы проходят
3. Убедитесь, что `NEXT_PUBLIC_API_URL` установлен правильно на Vercel

### Ошибка сборки на Vercel

1. Проверьте, что `Root Directory` = `apps/web`
2. Проверьте, что `Build Command` правильный
3. Проверьте логи сборки в Vercel (Build Logs)

## 📝 Итоговые настройки

### Vercel Settings:

```
Root Directory: apps/web
Build Command: cd ../.. && pnpm install && pnpm --filter @fadearena/web build
Output Directory: .next
Install Command: pnpm install
```

### Environment Variables на Vercel:

```
NEXT_PUBLIC_API_URL = https://fadearena-api.fly.dev
```

### Fly.io (уже настроено):

- ✅ URL: `https://fadearena-api.fly.dev`
- ✅ Переменные окружения установлены
- ✅ Машина запущена

## 🎉 Готово!

После успешного деплоя ваш сайт будет доступен по адресу:
- **Production:** `https://your-project.vercel.app`
- **Preview:** `https://your-project-*.vercel.app` (для каждого PR)

API работает через: `https://fadearena-api.fly.dev`

---

## 💡 Полезные команды

### Проверка статуса API:
```powershell
flyctl status --app fadearena-api
```

### Просмотр логов API:
```powershell
flyctl logs --app fadearena-api
```

### Перезапуск API (если нужно):
```powershell
flyctl apps restart fadearena-api
```

