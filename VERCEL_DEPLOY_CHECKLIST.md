# ✅ Чеклист для деплоя на Vercel

## ✅ Проверено и готово:

1. **✅ Сборка проходит успешно**
   - `pnpm build` выполняется без ошибок
   - Все TypeScript ошибки исправлены

2. **✅ Все API вызовы удалены**
   - Компоненты используют статические данные
   - Нет зависимостей от внешнего API

3. **✅ Статические данные на месте**
   - `apps/web/data/staticData.ts` - все позиции
   - Чарт генерирует данные с колебаниями
   - Все компоненты работают без API

4. **✅ Конфигурация Vercel**
   - `apps/web/vercel.json` настроен
   - `apps/web/next.config.js` готов
   - Favicon заменен на logo.png

5. **✅ Компоненты обновлены**
   - PositionsSidebar - статические данные
   - FaderEquityChart - автообновление каждые 10 сек
   - AssetPrices - статические цены
   - BotTable - статические данные ботов
   - TradeFeed - пустой список
   - TopNav - статическое состояние

## 📋 Инструкция по деплою:

### 1. Закоммитьте изменения:
```bash
git add .
git commit -m "Static version ready for Vercel deployment"
git push
```

### 2. Настройки на Vercel:

**Root Directory:** `apps/web`

**Build Command:** `cd ../.. && pnpm install && pnpm --filter @fadearena/web build`

**Output Directory:** `.next`

**Install Command:** `pnpm install`

**Environment Variables:** НЕ НУЖНЫ (все статическое)

### 3. Проверка после деплоя:

- ✅ Лендинг открывается
- ✅ Логотип отображается
- ✅ Чарт показывает данные с колебаниями
- ✅ Позиции отображаются
- ✅ Дашборд работает
- ✅ Все страницы загружаются

## 🎉 Готово к деплою!

