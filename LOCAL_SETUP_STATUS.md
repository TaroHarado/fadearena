# Статус локальной настройки FadeArena

## ✅ Выполнено

1. **Зависимости установлены**
   - ✅ pnpm установлен (v10.22.0)
   - ✅ Все пакеты установлены через `pnpm install`

2. **База данных настроена**
   - ✅ Схема Prisma адаптирована для SQLite
   - ✅ Все Json поля заменены на String (JSON хранится как строка)
   - ✅ Все Decimal поля заменены на String (числа хранятся как строки)
   - ✅ Миграция выполнена успешно
   - ✅ База данных создана: `packages/shared/prisma/dev.db`
   - ✅ Начальные данные заполнены (seed)

3. **Код обновлен для SQLite**
   - ✅ Seed скрипт сериализует JSON в строки
   - ✅ API routes парсят JSON строки при чтении
   - ✅ API routes сериализуют JSON при записи
   - ✅ Worker сохраняет данные как строки
   - ✅ Worker парсит JSON строки при чтении

4. **Environment файл**
   - ✅ `.env` создан с SQLite DATABASE_URL
   - ✅ FADEARENA_MODE=simulation
   - ✅ Все необходимые переменные настроены

## 🚀 Запуск

Сервисы запущены в фоне командой `pnpm dev`.

Проверьте:
```bash
# Health check
curl http://localhost:3001/api/health

# Откройте браузер
start http://localhost:3000
```

## 📝 Важные замечания

### SQLite vs PostgreSQL

Текущая настройка использует **SQLite** для быстрого запуска без установки PostgreSQL. Это означает:

- ✅ Работает без Docker или локального PostgreSQL
- ✅ База данных - один файл: `packages/shared/prisma/dev.db`
- ⚠️ JSON хранится как строки (нужно парсить)
- ⚠️ Числа хранятся как строки (Number() конвертирует)
- ⚠️ Некоторые ограничения по сравнению с PostgreSQL

### Для production

Рекомендуется использовать PostgreSQL:
1. Установите PostgreSQL или используйте Docker
2. Измените `packages/shared/prisma/schema.prisma` обратно на PostgreSQL
3. Обновите `.env` с правильным DATABASE_URL
4. Запустите миграции заново

## 🔍 Проверка работы

1. **API должен отвечать:**
   ```bash
   curl http://localhost:3001/api/health
   ```

2. **Frontend должен открываться:**
   - http://localhost:3000

3. **Worker должен обновлять heartbeat:**
   - Проверьте через health endpoint
   - Должен показывать `worker.status: "ok"`

## 📂 Файлы базы данных

- База данных: `packages/shared/prisma/dev.db`
- Миграции: `packages/shared/prisma/migrations/`

## Следующие шаги

1. Откройте http://localhost:3000 в браузере
2. Проверьте что все табы загружаются
3. Проверьте health endpoint
4. Настройте bot wallet addresses для реальных данных

