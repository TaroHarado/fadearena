# 🚀 Деплой на Vercel - Полная инструкция

## ✅ Ваш API на Fly.io

**URL вашего API:** `https://fadearena-api.fly.dev`

**Статус:** Машина запущена, но health check не проходит (возможно, нужны переменные окружения)

## 📋 Шаг 1: Проверка и настройка API на Fly.io

### 1.1 Проверьте переменные окружения на Fly.io

```powershell
# Посмотреть все переменные
flyctl secrets list --app fadearena-api

# Если переменных нет, добавьте их:
flyctl secrets set DATABASE_URL="your-database-url" --app fadearena-api
flyctl secrets set MY_GEMINI_FADE_WALLET="0x..." --app fadearena-api
flyctl secrets set MY_GROK_FADE_WALLET="0x..." --app fadearena-api
# ... и так далее для всех кошельков
```

### 1.2 Проверьте логи API

```powershell
flyctl logs --app fadearena-api
```

Если видите ошибки подключения к БД или другие проблемы - исправьте их.

### 1.3 Проверьте, что API отвечает

```powershell
# Проверка health endpoint
Invoke-WebRequest -Uri "https://fadearena-api.fly.dev/api/health"
```

Если API не отвечает, проверьте логи и переменные окружения.

## 📋 Шаг 2: Деплой Next.js на Vercel

### 2.1 Подготовка

1. Убедитесь, что код закоммичен в Git
2. Зайдите на [vercel.com](https://vercel.com) и войдите

### 2.2 Создание проекта

1. Нажмите **"Add New Project"**
2. Импортируйте ваш Git репозиторий
3. Выберите репозиторий и нажмите **"Import"**

### 2.3 Настройка проекта

**⚠️ ВАЖНО:** Настройте следующие параметры:

- **Framework Preset**: `Next.js` (автоматически определится)
- **Root Directory**: `apps/web` ⚠️ **ОБЯЗАТЕЛЬНО!**
- **Build Command**: `cd ../.. && pnpm install && pnpm --filter @fadearena/web build`
- **Output Directory**: `.next` (по умолчанию)
- **Install Command**: `pnpm install` (по умолчанию)

### 2.4 Переменные окружения

Нажмите **"Environment Variables"** и добавьте:

```
NEXT_PUBLIC_API_URL = https://fadearena-api.fly.dev
```

**⚠️ ВАЖНО:** 
- Не добавляйте `/api` в конце - это уже есть в коде
- URL должен быть `https://fadearena-api.fly.dev` (без слэша в конце)

### 2.5 Деплой

Нажмите **"Deploy"** и дождитесь завершения сборки.

## 📋 Шаг 3: Проверка после деплоя

### 3.1 Проверьте лендинг

Откройте `https://your-project.vercel.app` - должен открыться лендинг с логотипом.

### 3.2 Проверьте дашборд

Откройте `https://your-project.vercel.app/dashboard` - должен открыться дашборд.

### 3.3 Проверьте API запросы

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу дашборда
4. Проверьте, что запросы идут на `https://fadearena-api.fly.dev/api/...`
5. Убедитесь, что запросы возвращают данные (статус 200)

### 3.4 Проверьте чарты

- Чарт Fader Equity должен загружаться
- Данные должны обновляться
- Позиции должны отображаться

## 🔧 Решение проблем

### API не отвечает (404/500)

1. Проверьте логи Fly.io: `flyctl logs --app fadearena-api`
2. Проверьте переменные окружения: `flyctl secrets list --app fadearena-api`
3. Проверьте, что машина запущена: `flyctl status --app fadearena-api`

### CORS ошибки

Если видите CORS ошибки в консоли браузера, добавьте в `packages/api/src/index.ts`:

```typescript
app.use(cors({
  origin: [
    'https://your-project.vercel.app',
    'https://your-project-*.vercel.app', // для preview deployments
  ],
  credentials: true,
}));
```

### Чарты не загружаются

1. Проверьте консоль браузера на ошибки
2. Проверьте Network tab - все ли запросы проходят
3. Убедитесь, что `NEXT_PUBLIC_API_URL` установлен правильно

### Ошибка сборки на Vercel

1. Проверьте, что `Root Directory` = `apps/web`
2. Проверьте, что `Build Command` правильный
3. Проверьте логи сборки в Vercel

## 📝 Итоговая конфигурация

### Vercel Settings:

```
Root Directory: apps/web
Build Command: cd ../.. && pnpm install && pnpm --filter @fadearena/web build
Output Directory: .next
Install Command: pnpm install
```

### Environment Variables на Vercel:

```
NEXT_PUBLIC_API_URL = https://fadearena-api.fly.dev
```

### Environment Variables на Fly.io:

Все переменные из вашего `.env` файла (DATABASE_URL, MY_*_FADE_WALLET, и т.д.)

## 🎉 Готово!

После успешного деплоя ваш сайт будет доступен по адресу:
- Production: `https://your-project.vercel.app`
- Preview: `https://your-project-*.vercel.app` (для каждого PR)

API будет работать через `https://fadearena-api.fly.dev`

