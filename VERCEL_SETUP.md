# 🚀 Деплой на Vercel - Пошаговая инструкция

## 📋 Предварительные требования

1. ✅ Git репозиторий с вашим кодом (GitHub, GitLab, Bitbucket)
2. ✅ Аккаунт на [Vercel](https://vercel.com)
3. ✅ API сервер развернут отдельно (Railway, Fly.io, или другой сервис)

## 🎯 Вариант: Деплой Next.js на Vercel + API отдельно (Рекомендуется)

### Шаг 1: Подготовка API сервера

1. Деплойте API на отдельном сервисе (Railway/Fly.io):
   ```bash
   # Следуйте инструкциям в FLY_DEPLOYMENT.md
   # или используйте Railway
   ```

2. Получите URL вашего API (например: `https://fadearena-api.railway.app`)

### Шаг 2: Деплой Next.js на Vercel

1. **Зайдите на [vercel.com](https://vercel.com)** и войдите

2. **Нажмите "Add New Project"**

3. **Импортируйте ваш Git репозиторий**

4. **Настройте проект:**
   - **Framework Preset**: Next.js (автоматически определится)
   - **Root Directory**: `apps/web` ⚠️ **ВАЖНО!**
   - **Build Command**: `cd ../.. && pnpm install && pnpm --filter @fadearena/web build`
   - **Output Directory**: `.next` (по умолчанию)
   - **Install Command**: `pnpm install` (по умолчанию)

5. **Добавьте переменные окружения:**
   - Нажмите "Environment Variables"
   - Добавьте:
     ```
     NEXT_PUBLIC_API_URL = https://your-api-domain.com
     ```
     ⚠️ Замените `https://your-api-domain.com` на реальный URL вашего API

6. **Нажмите "Deploy"**

### Шаг 3: Проверка

После деплоя проверьте:
- ✅ Лендинг открывается: `https://your-project.vercel.app`
- ✅ Дашборд работает: `https://your-project.vercel.app/dashboard`
- ✅ API запросы проходят (откройте DevTools → Network)
- ✅ Чарты отображаются
- ✅ Данные обновляются

## 🔧 Настройка переменных окружения на Vercel

После первого деплоя вы можете изменить переменные:

1. Зайдите в проект на Vercel
2. Settings → Environment Variables
3. Добавьте/измените переменные:
   - `NEXT_PUBLIC_API_URL` - URL вашего API сервера

## 📝 Структура проекта для Vercel

```
your-repo/
├── apps/
│   └── web/          ← Vercel деплоит отсюда
│       ├── app/
│       ├── components/
│       ├── next.config.js
│       └── package.json
├── packages/
│   ├── api/          ← Деплоится отдельно
│   └── shared/
└── apps/web/vercel.json
```

## ⚠️ Важные замечания

1. **Root Directory**: Обязательно укажите `apps/web` в настройках Vercel
2. **Build Command**: Должен быть из корня проекта, чтобы pnpm workspace работал
3. **API URL**: Убедитесь, что API сервер доступен публично
4. **CORS**: Убедитесь, что API сервер разрешает запросы с вашего Vercel домена

## 🐛 Решение проблем

### Ошибка сборки

Если сборка падает:
1. Проверьте, что `Root Directory` = `apps/web`
2. Проверьте, что `Build Command` правильный
3. Проверьте логи сборки в Vercel

### API не работает

1. Проверьте `NEXT_PUBLIC_API_URL` в Environment Variables
2. Проверьте, что API сервер доступен
3. Проверьте CORS настройки API
4. Откройте DevTools → Network и проверьте запросы

### Чарты не загружаются

1. Проверьте, что API возвращает данные
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что переменные окружения установлены

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Monorepo Deployment](https://vercel.com/docs/monorepos)
